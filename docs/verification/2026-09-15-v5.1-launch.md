# v5.1 launch — 2026-09-15 10:13 WIB

PRD `docs/prd/PRD-AIDIT-OS-V5.1-HEMAT.md` §6 steps 1-9 done and committed
(`f41e44e`..`3242a45`, plus fixes `065bde3`, `b490559`). Conductor and ops
unpaused and started 10:12 WIB — the PRD §7 24-hour verification window
starts now. This note is the handoff for whoever checks it next.

## What's done

1. `conductor/status.mjs` — one-shot snapshot (PM2, board, busy heads, lanes,
   asks, today's usage, Claude account per config dir). Use this and
   `graphify query --budget` instead of raw ledger/file reads.
2. `config/lanes.json` v5.1: Claude restricted to conductor-decision/review;
   opus 0/day; kimi-k27 (Ollama) and kimi-k3 (Kimi Code) disabled; Codex
   pool-wide 30/day cap (was double-counted per-lane, fixed in `b490559`);
   lane rest period parses the reset clock time out of the error message.
3. Hermes: primary provider is the local Ollama daemon (`glm-5.2:cloud`),
   `fallback_providers` chain glm-5.1 → glm-5.3-flash → Nous free,
   `agent.max_turns` 40. New `hermes-cli` runtime in `lanes.mjs` invokes
   `hermes-agent/bin/hermes.js` directly via NODE22 (bypasses cmd.exe's
   8191-char line limit for an 8kB packet). Old custom Ollama tool loop
   removed.
4. RTK v0.49.0 installed to `D:/aidit-tools/rtk` (checksum-verified), wired
   into Claude headless (`rtk hook claude` in `guard.settings.json`), Codex
   (`~/.codex/AGENTS.md` prose), Hermes (`~/.hermes/plugins/rtk-rewrite`).
   `rtk gain` had no data yet at write time — needs real head/worker traffic.
5. graphify: graph built for this repo (2635 nodes, `graphify-out/`,
   gitignored, rebuild with `graphify update D:/AI/aidit-os-v5`), wiki
   exported (152 articles). `head.mjs` pulls `graphify query --budget 1500`
   into the packet and calls `graphify update` after every commit; `run.mjs`
   attaches a `--budget 600` hint per new/unassigned ticket (capped to 3).
   `.mcp.json` has graphify-mcp + gbrain serve + github (verified); paperclip/
   telegram/canva/caveman-engine deliberately left out — no verified server
   command found for them, and paperclip/telegram already have working
   direct clients. **Known gap:** PM2 `graphify-watch` crash-loops (missing
   Python `watchdog`, unresolved even after `pip install`) — removed from
   `ecosystem.config.cjs`; the update-after-commit call covers the same need.
6. Claude headless: `--setting-sources ""`, `--max-turns 20`. `CLAUDE_CONFIG_DIR`
   comes from the process env, not per-lane — the `conductor` PM2 app now
   carries `CLAUDE_CONFIG_DIR=D:/aidit-claude-machine`. **Confirmed live**
   (10:13 WIB ledger): a real conductor-decision escalation hit that config
   dir and correctly got `"Not logged in"`, then fell back to the GLM result
   — exactly the intended behavior until the machine account logs in
   Thursday. `ops/claude-machine.cmd` is the login helper for that morning.
   Kimi: `kimi-code/dispatch` model added to the existing
   `~/.kimi-code/config.toml` (65536 ctx, no always_thinking, reusing the
   already-authenticated provider rather than an unverifiable separate
   home dir); `runKimi()` implemented but **never exercised against a real
   call** — the account has had a 403 since 2026-09-14 with no known reset
   date (Ask sent, see below). Stays `disabled`.
7. Conductor: routine tick runs on GLM (`askGlm`, Ollama `/api/chat` with a
   JSON-schema `format`) instead of Claude; Sonnet only for a flagged hard
   decision, capped 5/day (opus 0/day). Tick skips the model call entirely
   when `{issues, queue, lanes, openAsks}` hash hasn't changed since last
   time. `nudgeHeads` removed (Paperclip's own wake-on-assign heartbeat
   covers it). Fixed the 422 `invalid_issue_disposition` bug (28x in the
   2026-09-14 audit): a failed run now only comments, never PATCHes
   `in_progress → todo`. Each head's system prompt carries one distilled
   superpowers pattern (measured 651-741 bytes, under the 1kB ceiling).
8. Token accounting: Claude and Hermes calls log `{tokens, costUsd}` on every
   `lane.run`/`claude.call` ledger row (Codex and Kimi don't yet — documented
   gaps, not guessed numbers). Evening report (19:00) gets one added
   plain-Indonesian line: calls per pool today.
9. `CLAUDE.md` (aidit-os-v5): new "Orchestrator session (v5.1)" section —
   Sonnet, ≤150 turns, `status.mjs`/`graphify query` instead of raw reads,
   `--dry` instead of restarting `conductor` to test, and the rule that once
   v5.1 passes and Conductor is unpaused, **this session stops writing code**.

## Two real bugs caught by this session's own --dry verification

Before unpausing, `node conductor/run.mjs --once --dry` was run (pause
briefly lifted for the side-effect-free dry call, then restored to the
owner's exact prior state each time — ledger has an honest pause/resume
pair for this, nothing hidden):

- GLM sometimes wraps its JSON `format` reply in ```` ```json ... ``` ````
  fences, breaking `JSON.parse`. Fixed in `askGlm` (`065bde3`) and retested
  clean.
- Codex pool double-counted (see #2 above), fixed in `b490559`.

## Owner Asks sent this session (Telegram)

- `claude-machine-login-2026-09-17` — login the machine Claude account
  Thursday 06:00 WIB via `ops/claude-machine.cmd`. Default if silent: keep
  using GLM as fallback, remind again Thursday morning.
- `kimi-reset-date-2026-09-15` — Kimi Code's monthly reset date is unknown.
  Default if silent: assume reset on the 1st of the month, retry then.
- `wsl2-docker-2026-09-14` was already asked and approved the night before
  this session (needs Bapak physically present; not re-asked).

## Live at launch (10:13 WIB ledger, real — not a dry run)

`glm.call conductor.routine ok:true` → `claude.call conductor.decision
configDir:D:/aidit-claude-machine "Not logged in"` (expected, correct
fallback) → GLM's decision applied, including sending the
`prd-sjs-v5-approval-2026-09-15` ask autonomously → `conductor.tick ok:true`
→ `head.start dept:ops issue:AID-17`. The pipeline built in this session
works end to end on the first real tick.

## What's NOT verified yet (needs the 24h window to actually pass)

PRD §7's numbers need real elapsed time, not something this session can
fabricate: Claude cache-read/day, ≤15 machine-account headless calls/day, no
session >150 turns, ≥3 tickets finished via Hermes/GLM in ≤40 steps, `rtk
gain` ≥50%, zero 120s timeouts, Codex 0 usage-limit hits on ≤30 tasks,
19:00 report carrying the pool-usage line for real.

## Next check-in (~24h from launch, i.e. around 2026-09-16 10:00 WIB)

1. `node conductor/status.mjs`, `rtk gain`, and a ledger scan for the §7
   numbers above. Write the verdict to `docs/verification/`.
2. If it passes: QA review AID-6..9, execute the WSL2/Docker ask when Bapak
   is present, continue SJS tickets toward the 2026-09-21 cutover.
3. If it doesn't: fix the specific gap, keep the system running (or pause
   again if something is actively wrong — see PRD §2c's own fallback table
   first, most gaps there are supposed to self-heal), re-check in another
   24h window rather than re-running this whole build-out.
