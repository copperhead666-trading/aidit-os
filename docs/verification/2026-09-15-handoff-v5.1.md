# Handoff — sesi baru v5.1 (ditulis 2026-09-15 08:15 WIB)

Baca dulu, urut: (1) `docs/prd/PRD-AIDIT-OS-V5.1-HEMAT.md`, (2) file ini, (3) `conductor/README.md`
(ditulis lane semalam; ada di worktree `state/workspaces/internal/aid-3`, belum di-merge).
Jangan baca PRD v5/v4 lama, ledger mentah, atau kode kecuali file yang disebut di sini.

## Keadaan mesin

- Repo: `D:/AI/aidit-os-v5`, branch `v5` = `0bdbdf7`, remote `copperhead666-trading/aidit-os` (sudah push
  sampai `b7eb0c6`; commit setelahnya belum). Node 22 pinned `D:/aidit-node/node-v22.14.0-win-x64/node.exe`
  (Paperclip saja memakai node sistem v26).
- PM2: `aidit-v5` (cockpit 4200, Tailscale serve `/` → 4200) **online**; `paperclip-v5` (3120) **online**;
  `telegram` (long-poll, tanpa LLM) **online**; `conductor` **stopped**; `ops` **stopped**.
  Flag `state/pause.json` = paused (dipasang atas /pause owner 06:04). Jangan nyalakan conductor/ops
  sebelum PRD v5.1 §6 langkah 1–9 selesai dan terverifikasi.
- Paperclip: company `4adffa70-…` (config/paperclip.json), prefix AID. Semua kepala `adapterType process`
  → `node conductor/head.mjs --department <id>`, `maxConcurrentRuns 1`, heartbeat off (wake-on-assign).
- Papan (08:10 WIB): AID-1/2 EPIC; AID-4 PRD SJS **in_review** (SETUJU owner; file
  `state/workspaces/sjs-superapps/docs/tasks/PRD-AID-4-SJS-SUPERAPPS-V5.md`, branch `aid/aid-4` di klone lama);
  AID-6..9 (TICKET-01..04) **in_review** via claude-sonnet 03:58–04:23, commit di
  `state/workspaces/sjs-superapps/engineering` branch `aid/aid-6..9` (belum di-review QA, belum push);
  AID-14 in_review (design); AID-5/17/18 (ops, bug runner 422) dan AID-10..13/15/16 **blocked**
  (Paperclip menandai blocked otomatis saat run gagal).
- Bug runner yang diketahui: PATCH status `todo`/`blocked` dari `in_progress` ditolak 422
  `invalid_issue_disposition` (28×). Perbaiki di `conductor/head.mjs`: saat gagal, hanya komentar +
  biarkan status; jangan PATCH status kecuali `in_progress` → `in_review`.
- Report 07:00 pertama v5 **terkirim** (state/reports.json). Semua 4 Ask semalam SETUJU
  (AID-1→Engineering, WSL2/Docker, PRD SJS, bersih-bersih Lenovo) — belum ada yang dieksekusi.
- Rahasia: `.env.local` (TELEGRAM_BOT_TOKEN_AHMAD, TELEGRAM_OWNER_CHAT_ID, GITHUB_TOKEN),
  `config/paperclip-db-password.local.txt`, `.paperclip/instances/default/{config.json,.env}` — semua
  gitignored. Muat env dengan `loadEnvLocal()` dari `conductor/lib.mjs`.
- Kuota: Claude akun #1 (login di `~/.claude` mesin ini) = **mesin**; akun #2 = Bapak lewat
  `CLAUDE_CONFIG_DIR=D:\aidit-claude-owner` (belum dibuat). Codex Plus OK. Ollama Cloud $20 OK
  (semua lane `ready` pagi ini). Kimi Code 403 sampai reset (tanggal belum diketahui).
- Alat terpasang: `claude` 2.1.270, `codex` 0.153.4, `kimi`, `hermes` 0.21 (login Nous, model
  `upstage/solar-pro4:free`), `ollama` 0.33.2 (kimi-k2.7-code, glm-5.2/5.1/5.3-flash cloud),
  `graphify` (`~/.local/bin`), plugin caveman + superpowers + ruflo di Claude Code. Belum: RTK, opencode.
- Yang dibawa ke v5 dan sudah ada: `docs/brand/**`, `scripts/design/**`, `ops/voice/*`,
  `config/persona/*` (lexicon whole-word fix 2026-09-15), `config/company.json`, `config/lanes.json`,
  `conductor/{run,head,lanes,claude,owner,telegram,ops,guard,lib,telegram-client}.mjs`,
  `ops/paperclip-bootstrap.mjs`, `ecosystem.config.cjs`.

## Angka audit (bukti, jangan diulang)

Claude orkestrator interaktif 864 juta cache-read/2 hari (1 sesi 1.203 turn); lane Claude headless
20–40 juta/run; Kimi Code 22,6 juta (94 % cache-read, 25–36 langkah/sesi); GLM+Kimi via harness HATTA
61 juta, 48/68 gagal (timeout 120 s, iteration ceiling, retry buta); Codex 103 tugas 90 % sukses
70 K/tugas. Skrip audit: `E:/Temp/claude/.../scratchpad/{audit-claude,audit-codex-claude,kimi-usage2}.mjs`
(salin ke `ops/audit/` bila mau dipakai Finance).

## Urutan kerja sesi baru (= PRD v5.1 §6, orkestrator interaktif boleh menulis kode sampai selesai)

1. `conductor/status.mjs` (ringkasan ≤ 20 baris: PM2, papan per status, kepala sibuk, lane, ask,
   pemakaian hari ini) → dipakai tiap wakeup, bukan ledger mentah.
2. `config/lanes.json` v5.1: rantai §2c/§3; `kimi-k27` dan `kimi-k3` `disabled`; Claude hanya
   `conductor-decision` dan `review`; Codex pagu 30/hari; parser jam reset dari pesan error.
3. Hermes: provider custom Ollama (`http://127.0.0.1:11434/v1`, glm-5.2:cloud) + `hermes fallback add`
   glm-5.1 → glm-5.3-flash → Nous; runtime `hermes-cli` di `lanes.mjs` (`hermes --yolo -z <paket> --in <ws>
   --usage-file <tmp>`); hapus `runOllama`; semaphore 2; retry 3× untuk 429/503. Uji 1 tiket internal.
4. RTK: instal biner Windows; wrapper untuk Bash lane Claude (guard tetap), Codex, Hermes; `rtk gain`.
5. Graphify: `graphify D:/AI/aidit-os-v5 --wiki`, klone SJS (`state/workspaces/sjs-superapps/engineering`),
   Caveman; `--mcp` di `.mcp.json` v5 (github, gbrain, paperclip, canva, telegram, graphify, caveman
   engine); PM2 `graphify-watch`; `head.mjs` paket ≤ 8 kB via `query --budget 1500`; `run.mjs` ringkasan
   + `query --budget 600`.
6. Claude headless: `--setting-sources ""`, `--max-turns 20`, sonnet; `ops/claude-owner.cmd`
   (CLAUDE_CONFIG_DIR) untuk sesi Bapak. Codex: `--ephemeral`, resting sampai jam reset. Kimi:
   `dispatch/kimi-home` v5 (`max_context_size 65536`, tanpa `always_thinking`, hook guard, 40 langkah,
   tanpa retry), runtime `kimi-cli` membaca `wire.jsonl` untuk token; tetap `disabled`.
7. Conductor: tick hanya bila hash berubah; routing deterministik (tier 1 tanpa LLM); rutin GLM-5.2 JSON;
   Claude ≤ 5/hari, opus 0; hapus `nudgeHeads`; system prompt kepala ≤ 1 kB (pola superpowers);
   perbaiki bug 422 di `head.mjs`.
8. Pencatatan token per pool ke ledger + baris pemakaian di Report 19:00.
9. `CLAUDE.md` v5: aturan sesi orkestrator (Sonnet, ≤ 150 turn, status.mjs, graphify, tidak buka file
   mentah; setelah v5.1 lulus orkestrator tidak menulis kode lagi).
10. Verifikasi PRD §7, lalu **baru**: `setPaused(false)`, `pm2 start conductor ops`, QA review AID-6..9,
    eksekusi 3 Ask yang SETUJU (WSL2/Docker butuh Bapak hadir; bersih-bersih: keranjang sampah + temp
    saja dulu), lanjut tiket SJS (cutover 21 Sep).

## Jangan ulangi

Jangan bangun harness/tool-loop sendiri (pakai Hermes/Codex/Claude/Kimi CLI apa adanya). Jangan beri lane
paket > 8 kB atau akses Glob/list_directory bebas. Jangan retry tugas yang mentok langkah. Jangan biarkan
kepala non-Claude jatuh ke Claude. Jangan jalankan > 2 run Ollama serentak. Jangan restart conductor
untuk menguji (tiap restart = tick = panggilan model); pakai `--once --dry`. Jangan `--bare` (login hilang).
Jangan taruh backslash/backtick di `bash -c`/heredoc tanpa Write tool. Jangan buka file mentah di sesi
orkestrator; pakai `status.mjs` dan `graphify query`. Semua waktu WIB via `node -e` Asia/Jakarta.
